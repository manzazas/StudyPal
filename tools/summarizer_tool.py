def get_summary(notes :str) -> str:
    """Summarizes the notes into simpler and more concise terms.

    Args:
        notes (str) : A text that could be from a slide, an article, a document, or study notes.

    Returns:
        str: A bulleted string containing a summarized and simplified version of the text

    """

    print (f"---Tool: get_summary called for notes") #Log tool execution
    return notes
